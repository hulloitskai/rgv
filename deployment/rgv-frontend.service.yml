---

apiVersion: v1
kind: Service

metadata:
  name: rgv-frontend
  namespace: default

spec:
  type: ClusterIP
  selector:
    app: rgv-frontend
    tier: frontend
    track: stable
  ports:
    - name: http
      protocol: TCP
      port: 80
