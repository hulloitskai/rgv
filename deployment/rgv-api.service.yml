---

apiVersion: v1
kind: Service

metadata:
  name: rgv-api
  namespace: default

spec:
  type: ClusterIP
  selector:
    app: rgv-api
    tier: backend
    track: stable
  ports:
    - name: api
      protocol: TCP
      port: 3000
